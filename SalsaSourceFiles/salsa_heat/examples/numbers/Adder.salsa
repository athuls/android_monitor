module examples.numbers;

behavior Adder {

    Number number;

    Adder(Number number){
	this.number = number;
    }

    void go(){
	for (int i = 0; i< 1000; i++){
	    number<-inc()@currentContinuation;
	}
	number<-inc()@standardOutput<-println(token);
    }

}
